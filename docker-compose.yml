services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fin-app
    ports:
      - "5444:5444"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    environment:
      - NODE_ENV=development
